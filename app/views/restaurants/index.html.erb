<% content_for(:html_title) { "トップページ" } %>

<div class="container">
  <div class="row">
    <div class="col-md-9">
      <%= render 'index', news: @news, random: @random, wants: @wants, repeats: @repeats, scores: @scores %>
    </div>
    <div class="col-md-3">
      <%= render 'side', tags: @tags %>
    </div>
  </div>
</div>
